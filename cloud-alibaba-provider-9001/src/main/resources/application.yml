server:
  port: 9001

spring:
  application:
    name: nacos-provider-payment
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
#端点暴露
management:
  endpoints:
    web:
      exposure:
        include: "*"
